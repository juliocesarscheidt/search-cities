sudo: required
services:
  - docker

before_install:
  - docker image build -t juliocesarmidia/search-cities-front -f Dockerfile-front-dev .

script:
  - docker container run --name search-cities-front -p 8090:80 juliocesarmidia/search-cities-front npm run lint